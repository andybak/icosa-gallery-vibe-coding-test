<div align="center">
<img width="1200" height="475" alt="GHBanner" src="https://github.com/user-attachments/assets/0aa67016-6eaf-458a-adb2-6e31a0763ed6" />
</div>

# Run and deploy your AI Studio app

This contains everything you need to run your app locally.

View your app in AI Studio: https://ai.studio/apps/72a7fbc9-9460-4f9f-bba2-cd32f68c126c

## Run Locally

**Prerequisites:** Node.js

1. Install dependencies:
   `npm install`
2. Run the app:
   `npm run dev`

## Google Play Store (PWA/TWA) readiness

This repository now includes the parts we can safely automate in source control:

- A valid web manifest with local app icons and maskable icon metadata.
- Local text-based (non-binary) PWA icons in `public/icons/` to keep PR tooling compatible.
- Service worker registration and static service worker file.
- A Digital Asset Links template at `public/.well-known/assetlinks.example.json`.

### What you still need to do (manual)

These steps require your own domain, signing keys, and Play Console account.

#### Step 1 — Set up a custom domain (required)

GitHub Pages project sites are served from a subdirectory
(`https://andybak.github.io/icosa-gallery-vibe-coding-test/`). The Digital
Asset Links file (`assetlinks.json`) must be at the **root** of the domain
(`https://yourdomain.com/.well-known/assetlinks.json`), and you cannot control
the root of `andybak.github.io`. A custom domain is therefore required.

1. Register a domain (e.g. `icosa.yourdomain.com`).
2. In **GitHub → Settings → Pages**, add your custom domain and follow the DNS
   instructions.
3. In `vite.config.ts` / the GitHub Actions workflow, change `VITE_BASE_PATH`
   from `/<repo-name>/` to `/`.
4. In `public/manifest.json`, change `"id"`, `"start_url"`, and `"scope"` from
   `"."` to `"/"` now that the app is served from the domain root.

#### Step 2 — Create a Play Console developer account

If you don't already have one, register at
`https://play.google.com/console` (one-time $25 fee).

#### Step 3 — Build the Android TWA package with Bubblewrap

Bubblewrap reads your manifest and generates a signable Android project.

```bash
npm install -g @bubblewrap/cli
bubblewrap init --manifest https://yourdomain.com/manifest.json
bubblewrap build
```

`bubblewrap init` will:
- Ask for a package name (e.g. `com.yourcompany.icosaexplorer`)
- Generate a signing keystore if you don't already have one
- Print the **SHA-256 certificate fingerprint** — copy this, you need it next

#### Step 4 — Fill in assetlinks.json

`public/.well-known/assetlinks.json` already exists in this repo with
placeholder values. Replace them with your real values:

```json
{
  "package_name": "com.yourcompany.icosaexplorer",
  "sha256_cert_fingerprints": ["AA:BB:CC:..."]
}
```

Deploy the site and verify the file is publicly reachable:
```
https://yourdomain.com/.well-known/assetlinks.json
```

You can validate it with Google's tool:
`https://digitalassetlinks.googleapis.com/v1/statements:list?source.web.site=https://yourdomain.com&relation=delegate_permission/common.handle_all_urls`

#### Step 5 — Play Console: create the app listing

1. Create a new app in Play Console.
2. Fill in the store listing: title, description, screenshots, feature graphic.
3. Complete the **Data Safety** form.
4. Complete the **Content Rating** questionnaire.
5. Add a **Privacy Policy** URL.

#### Step 6 — Upload and test

1. Upload the `.aab` file generated by Bubblewrap to the **Internal Testing** track.
2. Install it on a real Android device via the internal testing link.
3. Confirm the app opens your production URL without a browser address bar
   (this only works once `assetlinks.json` is live and verified).
4. Run a Lighthouse PWA audit on your production URL and confirm no failures.

#### Step 7 — Promote to production

Once internal testing passes, promote the release to Production in Play Console.

### Suggested release checklist

- [ ] Custom domain configured and `VITE_BASE_PATH=/` in CI
- [ ] `manifest.json` `start_url` and `scope` updated to `/`
- [ ] `npm run lint` passes
- [ ] `npm run build` passes
- [ ] Manifest icons load from your custom domain
- [ ] `/.well-known/assetlinks.json` is live, valid JSON, and passes Google's validator
- [ ] Android package name + SHA-256 fingerprint in `assetlinks.json` match the Bubblewrap keystore
- [ ] Internal testing build installs and opens production URL without browser chrome
- [ ] Lighthouse PWA audit on production URL shows no failures
- [ ] Play Console store listing, Data Safety, Content Rating, and Privacy Policy all complete

## GitHub Pages CI publishing

This repo now includes a GitHub Actions workflow at `.github/workflows/deploy-pages.yml` that:

- installs dependencies with `npm ci`
- runs `npm run lint`
- builds the app with a Pages base path (`VITE_BASE_PATH=/<repo-name>/`)
- deploys `dist/` to GitHub Pages on pushes to `main`

### One-time setup you must do in GitHub

1. Go to **Settings → Pages**.
2. Under **Build and deployment**, set **Source** to **GitHub Actions**.
3. Ensure your default branch is `main` (or update the workflow trigger branch).
4. Push to `main` (or run the workflow manually from the Actions tab).

### If you use a custom domain

- Set `VITE_BASE_PATH=/` in the workflow build step instead of `/<repo-name>/`.
- Configure your domain in **Settings → Pages** and add DNS records.


### Note on icons

This repo commits PNG icons (`public/icons/icon-192x192.png`,
`icon-512x512.png`, `icon-512x512-maskable.png`) required by the Play Store,
alongside the source SVG files. They were generated from the SVGs using
`sharp`. If you update the SVG sources, regenerate the PNGs accordingly.
